torch>=2.0
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0

# dmg is pinned to a commit that includes the warm-up symmetry fix
# (mhpi/generic_deltamodel#112). Earlier pre-#112 dmg revisions silently
# crashed `calc_metrics` for any model whose forward returned the full
# (pre-warm-up) test window.
dmg @ git+https://github.com/mhpi/generic_deltamodel.git@05db58e89927072551b1519612671b9acd64a48a
# hydrodl2 is pinned to the exact commit used to train and verify the
# checkpoints shipped under weights/. Newer revisions are likely fine now
# that the dmg-side metric alignment is symmetric, but unpinning has not
# been re-verified end-to-end against these specific weights.
hydrodl2 @ git+https://github.com/mhpi/hydrodl2.git@769da56f6
